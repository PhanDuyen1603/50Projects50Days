const tagsEl = document.getElementById("tags");
const textarea = document.getElementById("textarea");

textarea.focus();

textarea.addEventListener('keyup', (e)=>{
  
})

function createTags(input){
  const tags = input.split(',').filters
}
